/*write a function which takes hours,minutes, and seconds as parameters and an integer s and increment the time by s seconds 
Accepts time and seconds in main and displays the new time in main.*/
#include<stdio.h>
int increment(int time)
{
	int e_min,e_hr,added_min,added_hr,sec,min,hour,added_sec,S;
	
	added_sec=sec+S;
	e_min=added_sec/60;
	sec=added_sec%60;
	added_min=e_min+min;
	e_hr=added_min/60;
	min=addeed_min%60;
	hour=e
}
